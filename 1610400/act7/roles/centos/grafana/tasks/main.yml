---
# tasks file for centos/grafana
name: Add grafana.repo
copy:
  src: grafana.repo
  dest: /etc/yum.repo.d/

 name: update centos
 yum:
   update_cache: yes
  
 name: install grafana
 yum:
   name: grafana
   state: present
