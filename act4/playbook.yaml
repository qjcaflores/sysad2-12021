---
name: Install java on ubuntu
hosts: ubuntu
  apt:
    name: default-jre
    state: present
    update_cache: yes


name: Install ajva
hosts: centos
  yum:
    name: java-1..8.0-openjdk
    update_cache: yes



name: Install using pip
hosts: all
  pip:
    name:
      - ansible
      - boto
      - python-openstackclient
